📸 Photo Status" name="group_photo_status"
                            context="{'group_by':'photos_complete'}"/>
                    <filter string="📅 Request Date" name="group_request_date"
                            context="{'group_by':'date_request'}"/>
                    <filter string="🔄 Return Type" name="group_return_type"
                            context="{'group_by':'return_type'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ✨ ENHANCED: Main Custody Action with Smart Defaults -->
    <record id="hr_custody_action" model="ir.actions.act_window">
        <field name="name">Custody Requests</field>
        <field name="res_model">hr.custody</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="hr_custody_view_search"/>
        <field name="context">{
            'search_default_waiting_my_approval': 1,
        }</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to Create a New Custody Request.
            </p>
            <p>
                <strong>🔥 Smart Filters Available:</strong><br/>
                • <strong>🔔 Waiting My Approval</strong> - See requests needing your approval<br/>
                • <strong>📋 My Requests</strong> - View your own requests<br/>
                • <strong>⚠️ Overdue Items</strong> - Critical items past return date<br/>
                • <strong>🕐 Due This Week</strong> - Items due soon<br/>
                • <strong>📸 Photo Status</strong> - Track photo documentation
            </p>
        </field>
    </record>

    <!-- 🎯 SINGLE UNIFIED MENU with Smart Filters -->
    <menuitem action="hr_custody_action"
              id="hr_custody_menu_unified"
              parent="hr_custody_menu_management"
              name="📋 Custody Requests"
              sequence="1"/>

    <!-- Properties menu -->
    <menuitem action="custody_property_action"
              id="hr_property_menu"
              parent="hr_custody_menu_management"
              name="🏢 Properties"
              sequence="5"
              groups="hr.group_hr_manager"/>
</odoo>