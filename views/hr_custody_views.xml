<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Hr Custody form view - Updated for Odoo 18 -->
    <record id="hr_custody_view_form" model="ir.ui.view">
        <field name="name">hr.custody.form</field>
        <field name="model">hr.custody</field>
        <field name="arch" type="xml">
            <form string="Custody">
                <header>
                    <button string="Send For Approval" type="object"
                            name="sent"
                            class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button string="Approve" groups="hr.group_hr_manager"
                            type="object" name="approve"
                            class="oe_highlight"
                            invisible="state != 'to_approve' or is_renew_return_date"/>
                    <button string="Renewal Approval"
                            groups="hr.group_hr_manager" type="object"
                            name="renew_approve"
                            class="oe_highlight"
                            invisible="state != 'to_approve' or not is_renew_return_date"/>
                    <button string="Refuse" groups="hr.group_hr_manager"
                            type="action"
                            name="%(hr_custody.wizard_reason_act)d"
                            invisible="state != 'to_approve' or is_renew_return_date"
                            context="{'reject_id':id,'model_id':'hr.custody'}"/>
                    <button class="btn-primary"
                            name="%(hr_custody.wizard_reason_act)d"
                            string="Refuse"
                            groups="hr.group_hr_manager" type="action"
                            invisible="state != 'to_approve' or not is_renew_return_date"
                            context="{'reject_id':id,'model_id':'hr.custody','renew': 'renew'}"/>
                    <button string="Set to Draft" type="object"
                            name="set_to_draft"
                            invisible="state != 'rejected'"/>
                    <button string="Return" type="object"
                            name="set_to_return"
                            groups="hr.group_hr_manager"
                            invisible="state != 'approved'"/>
                    <button string="Send Mail" type="object"
                            name="send_mail" groups="hr.group_hr_manager"
                            invisible="not is_mail_send or state != 'approved'"/>
                    <button string="Send Mail" type="object"
                            class="oe_highlight" name="send_mail"
                            groups="hr.group_hr_manager"
                            invisible="is_mail_send or state != 'approved'"/>
                    <button string="Renew" type="action"
                            name="%(property_return_date_act)d"
                            context="{'custody_id':id}"
                            class="oe_highlight"
                            invisible="state != 'approved' or is_renew_return_date"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,to_approve,approved,returned"/>
                </header>
                <sheet>
                    <h1>
                        <field name="name" readonly="1" nolabel="1"/>
                    </h1>
                    <group>
                        <group>
                            <field name="is_read_only" invisible="1"/>
                            <field name="employee_id"
                                   readonly="not is_read_only"/>
                            <field name="custody_property_id"
                                   options="{'no_open':True,'no_create':True}"
                                   domain="[('company_id','child_of',[company_id])]"
                                   readonly="state != 'draft'"/>
                            <field name="is_renew_return_date" invisible="1"/>
                            <field name="is_renew_reject" invisible="1"/>
                            <field name="is_mail_send" invisible="1"/>
                            <field name="purpose" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="date_request" readonly="state != 'draft'"/>
                            <field name="return_date"
                                   invisible="is_renew_return_date and renew_date and not is_renew_reject"
                                   readonly="state != 'draft'"/>
                            <field name="renew_date"
                                   invisible="not is_renew_return_date or state != 'to_approve'"/>
                            <field name="rejected_reason"
                                   invisible="state != 'rejected'"/>
                            <field name="renew_rejected_reason"
                                   invisible="not is_renew_reject or state != 'approved'"/>
                            <field name="company_id"
                                   options="{'no_create': True}"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Notes">
                            <field name="notes"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Hr Custody list view - Updated for Odoo 18 (tree -> list) -->
    <record id="hr_custody_view_tree" model="ir.ui.view">
        <field name="name">hr.custody.list</field>
        <field name="model">hr.custody</field>
        <field name="arch" type="xml">
            <list string="Custody" multi_edit="1">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="custody_property_id"/>
                <field name="purpose"/>
                <field name="date_request"/>
                <field name="return_date"/>
                <field name="state" decoration-success="state == 'approved'"
                       decoration-info="state == 'to_approve'"
                       decoration-muted="state == 'returned'"
                       decoration-danger="state == 'rejected'"
                       widget="badge"/>
            </list>
        </field>
    </record>

    <!-- Hr Custody search view - Updated for Odoo 18 -->
    <record id="hr_custody_view_search" model="ir.ui.view">
        <field name="name">hr.custody.search</field>
        <field name="model">hr.custody</field>
        <field name="arch" type="xml">
            <search string="Custody">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="custody_property_id"/>
                <field name="purpose"/>
                <field name="date_request"/>
                <field name="return_date"/>
                <field name="state"/>

                <!-- Filters -->
                <filter string="My Custodies" name="my_custodies"
                        domain="[('employee_id.user_id', '=', uid)]"/>
                <filter string="Draft" name="draft"
                        domain="[('state', '=', 'draft')]"/>
                <filter string="Waiting Approval" name="to_approve"
                        domain="[('state', '=', 'to_approve')]"/>
                <filter string="Approved" name="approved"
                        domain="[('state', '=', 'approved')]"/>
                <filter string="Returned" name="returned"
                        domain="[('state', '=', 'returned')]"/>
                <filter string="Overdue" name="overdue"
                        domain="[('return_date', '&lt;', context_today().strftime('%Y-%m-%d')), ('state', '=', 'approved')]"/>

                <separator/>

                <!-- Date filters -->
                <filter string="This Month" name="this_month"
                        domain="[('date_request', '&gt;=', context_today().replace(day=1).strftime('%Y-%m-%d'))]"/>
                <filter string="This Year" name="this_year"
                        domain="[('date_request', '&gt;=', context_today().replace(month=1, day=1).strftime('%Y-%m-%d'))]"/>

                <separator/>

                <!-- Group By -->
                <group expand="0" string="Group By">
                    <filter string="Status" name="group_status"
                            context="{'group_by':'state'}"/>
                    <filter string="Employee" name="group_employee"
                            context="{'group_by':'employee_id'}"/>
                    <filter string="Property" name="group_property"
                            context="{'group_by':'custody_property_id'}"/>
                    <filter string="Request Date" name="group_request_date"
                            context="{'group_by':'date_request'}"/>
                    <filter string="Return Date" name="group_return_date"
                            context="{'group_by':'return_date'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Hr Custody kanban view - New for Odoo 18 -->
    <record id="hr_custody_view_kanban" model="ir.ui.view">
        <field name="name">hr.custody.kanban</field>
        <field name="model">hr.custody</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" class="o_kanban_small_column" quick_create="false">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="custody_property_id"/>
                <field name="purpose"/>
                <field name="date_request"/>
                <field name="return_date"/>
                <field name="state"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <field name="name"/>
                                    </strong>
                                </div>
                                <div class="o_kanban_record_top_right">
                                    <div class="o_dropdown_kanban dropdown">
                                        <a role="button" class="dropdown-toggle o-no-caret btn" data-bs-toggle="dropdown" href="#" aria-label="Dropdown menu" title="Dropdown menu">
                                            <span class="fa fa-ellipsis-v"/>
                                        </a>
                                        <div class="dropdown-menu" role="menu">
                                            <a t-if="widget.editable" role="menuitem" type="edit" class="dropdown-item">Edit</a>
                                            <a t-if="widget.deletable" role="menuitem" type="delete" class="dropdown-item">Delete</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="o_kanban_record_body">
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_left">
                                        <span class="o_kanban_record_subtitle">
                                            <i class="fa fa-user"/> <field name="employee_id"/>
                                        </span>
                                        <br/>
                                        <span class="o_kanban_record_subtitle">
                                            <i class="fa fa-cube"/> <field name="custody_property_id"/>
                                        </span>
                                        <br/>
                                        <span class="o_kanban_record_subtitle">
                                            <i class="fa fa-calendar"/> Return: <field name="return_date"/>
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Hr Custody action - Updated for Odoo 18 -->
    <record id="hr_custody_action" model="ir.actions.act_window">
        <field name="name">Custody</field>
        <field name="res_model">hr.custody</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="search_view_id" ref="hr_custody_view_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first custody request!
            </p>
            <p>
                Track company assets and equipment assigned to employees.
            </p>
        </field>
        <field name="context">{
            'search_default_my_custodies': 1
        }</field>
    </record>

    <!-- Hr Custody menu -->
    <menuitem id="hr_custody_main_menu"
              web_icon="hr_custody,static/description/icon.png"
              name="Custody" sequence="20"/>

    <menuitem id="hr_custody_menu_management"
              parent="hr_custody_main_menu"
              name="Custody Management"
              sequence="10"/>

    <menuitem action="hr_custody_action"
              id="hr_custody_menu_request"
              parent="hr_custody_menu_management"
              name="Custody Request"
              sequence="1"/>

    <!-- Property menu item -->
    <menuitem action="custody_property_action"
              id="hr_property_menu"
              parent="hr_custody_menu_management"
              name="Properties"
              sequence="5"
              groups="hr.group_hr_manager"/>
</odoo>
