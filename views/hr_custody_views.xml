<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Hr Custody form view -->
    <record id="hr_custody_view_form" model="ir.ui.view">
        <field name="name">hr.custody.form</field>
        <field name="model">hr.custody</field>
        <field name="arch" type="xml">
            <form string="Custody">
                <header>
                    <button string="Send For Approval" type="object"
                            name="sent"
                            class="oe_highlight"
                            invisible="state != 'draft'"/>

                    <!-- ⭐ ปุ่มอนุมัติ - แสดงเฉพาะคนที่อยู่ใน property approvers -->
                    <button string="Approve" type="object" name="approve"
                            class="oe_highlight"
                            invisible="state != 'to_approve'"
                            groups="base.group_user"/>

                    <!-- ⭐ ปุ่มปฏิเสธ -->
                    <button string="Refuse" type="object" name="refuse_with_reason"
                            invisible="state != 'to_approve'"
                            groups="base.group_user"/>

                    <button string="Set to Draft" type="object"
                            name="set_to_draft"
                            invisible="state != 'rejected'"/>
                    <button string="Return" type="object"
                            name="set_to_return"
                            groups="hr.group_hr_manager"
                            invisible="state != 'approved'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,to_approve,approved,returned"/>
                </header>
                <sheet>
                    <!-- ✅ FIXED: Alert สำหรับ Overdue - เพิ่ม role="alert" -->
                    <div class="alert alert-danger" role="alert" invisible="not is_overdue or state == 'returned'">
                        <strong>⚠️ OVERDUE:</strong> This item is <field name="days_overdue" nolabel="1"/> days overdue!
                    </div>

                    <!-- ✅ FIXED: Alert สำหรับ Pending Approval - เพิ่ม role="alert" -->
                    <div class="alert alert-info" role="alert" invisible="state != 'to_approve'">
                        <strong>🔔 Waiting for approval from any of these users:</strong><br/>
                        <field name="property_approver_ids" widget="many2many_tags" readonly="1"/>
                    </div>

                    <!-- ✅ FIXED: Alert สำหรับ Current User ที่เป็น Approver - เพิ่ม role="alert" -->
                    <div class="alert alert-warning" role="alert" invisible="state != 'to_approve' or uid not in property_approver_ids">
                        <strong>⚡ This request is waiting for YOUR approval!</strong>
                    </div>

                    <!-- ✅ FIXED: Alert สำหรับ Approved Status - เพิ่ม role="alert" -->
                    <div class="alert alert-success" role="alert" invisible="state not in ['approved', 'returned']">
                        <strong>✅ Approved by:</strong>
                        <field name="approved_by_id" readonly="1" nolabel="1"/>
                        <strong>on</strong>
                        <field name="approved_date" readonly="1" nolabel="1"/>
                    </div>

                    <!-- ✅ FIXED: Alert สำหรับ Returned Status - เพิ่ม role="alert" -->
                    <div class="alert alert-success" role="alert" invisible="state != 'returned'">
                        <strong>📦 Returned:</strong>
                        Equipment returned on <field name="actual_return_date" readonly="1" nolabel="1"/>
                        by <field name="returned_by_id" readonly="1" nolabel="1"/>
                    </div>

                    <h1>
                        <field name="name" readonly="1" nolabel="1"/>
                    </h1>
                    <group>
                        <group>
                            <field name="employee_id"/>
                            <field name="custody_property_id"
                                   readonly="state != 'draft'"
                                   domain="[('property_status', '=', 'available')]"/>
                            <field name="purpose" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="date_request" readonly="state != 'draft'"/>
                            <field name="return_type" readonly="state != 'draft'" widget="radio"/>
                            <field name="return_date" readonly="state != 'draft'"
                                   invisible="return_type != 'date'"
                                   string="Expected Return Date"/>
                            <field name="expected_return_period" readonly="state != 'draft'"
                                   invisible="return_type == 'date'"/>
                            <field name="return_status_display" readonly="1"/>
                            <field name="rejected_reason"
                                   invisible="state != 'rejected'"/>

                            <!-- ⭐ Approval Information -->
                            <field name="approved_by_id"
                                   invisible="state not in ['approved', 'returned']"/>
                            <field name="approved_date"
                                   invisible="state not in ['approved', 'returned']"/>

                            <!-- ⭐ Return Information -->
                            <field name="actual_return_date"
                                   invisible="state != 'returned'"/>
                            <field name="returned_by_id"
                                   invisible="state != 'returned'"/>

                            <field name="company_id"
                                   options="{'no_create': True}"/>
                        </group>
                    </group>

                    <!-- ⭐ Hidden fields for permissions and overdue status -->
                    <field name="property_approver_ids" invisible="1"/>
                    <field name="is_overdue" invisible="1"/>
                    <field name="days_overdue" invisible="1"/>

                    <notebook>
                        <page string="Notes">
                            <field name="notes"/>
                        </page>

                        <!-- ⭐ NEW: Approval Information Tab -->
                        <page string="Approval Info" invisible="state == 'draft'">
                            <group>
                                <group string="Available Approvers">
                                    <field name="property_approver_ids" widget="many2many_tags" readonly="1"/>
                                </group>
                                <group string="Approval Details" invisible="state not in ['approved', 'returned']">
                                    <field name="approved_by_id" readonly="1"/>
                                    <field name="approved_date" readonly="1"/>
                                </group>
                            </group>
                            <group string="Return Details" invisible="state != 'returned'">
                                <group>
                                    <field name="actual_return_date" readonly="1"/>
                                    <field name="returned_by_id" readonly="1"/>
                                </group>
                                <group>
                                    <field name="return_notes" readonly="1"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Hr Custody list view -->
    <record id="hr_custody_view_tree" model="ir.ui.view">
        <field name="name">hr.custody.list</field>
        <field name="model">hr.custody</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="employee_id"/>
                <field name="custody_property_id"/>
                <field name="purpose"/>

                <!-- ⭐ Approvers &amp; Approved By Columns -->
                <field name="property_approver_ids" widget="many2many_tags" string="Approvers"/>
                <field name="approved_by_id" string="Approved By"/>

                <field name="date_request"/>
                <field name="return_type"/>
                <field name="return_date" string="Expected Return"/>
                <field name="actual_return_date" string="Actual Return"/>
                <field name="return_status_display"/>

                <!-- ⭐ Overdue indicator -->
                <field name="is_overdue" invisible="1"/>
                <field name="days_overdue" invisible="1"/>

                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-info="state == 'to_approve'"
                       decoration-muted="state == 'returned'"
                       decoration-danger="state == 'rejected' or is_overdue"/>
            </list>
        </field>
    </record>

    <!-- Hr Custody search view -->
    <record id="hr_custody_view_search" model="ir.ui.view">
        <field name="name">hr.custody.search</field>
        <field name="model">hr.custody</field>
        <field name="arch" type="xml">
            <search string="Custody">
                <field name="name" string="Code"/>
                <field name="employee_id"/>
                <field name="custody_property_id"/>
                <field name="purpose"/>

                <!-- ⭐ Search by Approvers &amp; Approved By -->
                <field name="property_approver_ids"/>
                <field name="approved_by_id"/>

                <field name="state"/>
                <field name="return_type"/>

                <!-- ⭐ Important Filters -->
                <filter string="🔔 Waiting My Approval" name="waiting_my_approval"
                        domain="[('property_approver_ids', 'in', [uid]), ('state', '=', 'to_approve')]"/>
                <filter string="📋 My Requests" name="my_requests"
                        domain="[('employee_id.user_id', '=', uid)]"/>
                <filter string="✅ Approved by Me" name="approved_by_me"
                        domain="[('approved_by_id', '=', uid)]"/>

                <separator/>

                <!-- ⭐ NEW: Overdue and Return Date Filters -->
                <filter string="⚠️ Overdue" name="overdue"
                        domain="[('is_overdue', '=', True)]"/>

                <!-- ✅ FIXED: XML Syntax - ใช้ &amp; แทน & และ &gt; แทน > -->
                <filter string="📅 Returned This Week" name="returned_this_week"
                        domain="[('actual_return_date', '&gt;=', (context_today() - datetime.timedelta(days=7)).strftime('%Y-%m-%d'))]"/>
                <filter string="📅 Returned This Month" name="returned_this_month"
                        domain="[('actual_return_date', '&gt;=', (context_today().replace(day=1)).strftime('%Y-%m-%d'))]"/>

                <separator/>

                <!-- Original Filters -->
                <filter string="Draft" name="draft"
                        domain="[('state', '=', 'draft')]"/>
                <filter string="Waiting Approval" name="to_approve"
                        domain="[('state', '=', 'to_approve')]"/>
                <filter string="Approved" name="approved"
                        domain="[('state', '=', 'approved')]"/>
                <filter string="Returned" name="returned"
                        domain="[('state', '=', 'returned')]"/>

                <separator/>

                <filter string="Fixed Return Date" name="fixed_date"
                        domain="[('return_type', '=', 'date')]"/>
                <filter string="Flexible Return" name="flexible"
                        domain="[('return_type', '=', 'flexible')]"/>
                <filter string="Term End Return" name="term_end"
                        domain="[('return_type', '=', 'term_end')]"/>

                <separator/>

                <!-- Group By -->
                <group expand="0" string="Group By">
                    <filter string="Status" name="group_status"
                            context="{'group_by':'state'}"/>
                    <filter string="Employee" name="group_employee"
                            context="{'group_by':'employee_id'}"/>

                    <!-- ⭐ Group by Approved By -->
                    <filter string="Approved By" name="group_approved_by"
                            context="{'group_by':'approved_by_id'}"/>

                    <!-- ⭐ NEW: Group by Return Status -->
                    <filter string="Return Status" name="group_return_status"
                            context="{'group_by':'is_overdue'}"/>
                    <filter string="Actual Return Date" name="group_actual_return_date"
                            context="{'group_by':'actual_return_date'}"/>

                    <filter string="Return Type" name="group_return_type"
                            context="{'group_by':'return_type'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Hr Custody action -->
    <record id="hr_custody_action" model="ir.actions.act_window">
        <field name="name">Custody</field>
        <field name="res_model">hr.custody</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="hr_custody_view_search"/>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to Create a New Custody Request.
            </p>
        </field>
    </record>

    <!-- ⭐ NEW: Action สำหรับ Pending Approvals -->
    <record id="hr_custody_pending_approval_action" model="ir.actions.act_window">
        <field name="name">🔔 Pending My Approval</field>
        <field name="res_model">hr.custody</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_approver_ids', 'in', [uid]), ('state', '=', 'to_approve')]</field>
        <field name="context">{'search_default_waiting_my_approval': 1}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent">
                No custody requests waiting for your approval.
            </p>
        </field>
    </record>

    <!-- ⭐ NEW: Action สำหรับ Overdue Items -->
    <record id="hr_custody_overdue_action" model="ir.actions.act_window">
        <field name="name">⚠️ Overdue Items</field>
        <field name="res_model">hr.custody</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('is_overdue', '=', True)]</field>
        <field name="context">{'search_default_overdue': 1}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent">
                No overdue custody items found.
            </p>
        </field>
    </record>

    <!-- ===== MENU STRUCTURE ===== -->

    <!-- Main menu -->
    <menuitem id="hr_custody_main_menu"
              name="Custody"
              sequence="20"
              web_icon="hr_custody,static/description/icon.png"/>

    <!-- Management submenu -->
    <menuitem id="hr_custody_menu_management"
              parent="hr_custody_main_menu"
              name="Custody Management"
              sequence="10"/>

    <!-- Custody Request menu -->
    <menuitem action="hr_custody_action"
              id="hr_custody_menu_request"
              parent="hr_custody_menu_management"
              name="Custody Request"
              sequence="1"/>

    <!-- ⭐ NEW: Pending Approvals menu -->
    <menuitem action="hr_custody_pending_approval_action"
              id="hr_custody_menu_pending_approval"
              parent="hr_custody_menu_management"
              name="🔔 Pending My Approval"
              sequence="2"/>

    <!-- ⭐ NEW: Overdue Items menu -->
    <menuitem action="hr_custody_overdue_action"
              id="hr_custody_menu_overdue"
              parent="hr_custody_menu_management"
              name="⚠️ Overdue Items"
              sequence="3"/>

    <!-- Properties menu -->
    <menuitem action="custody_property_action"
              id="hr_property_menu"
              parent="hr_custody_menu_management"
              name="Properties"
              sequence="5"
              groups="hr.group_hr_manager"/>
</odoo>
